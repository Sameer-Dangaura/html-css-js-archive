> CSS Pseudo-elements
-----------------------

* pseudo => means not genuine;


> What are Pseudo-Elements?
:
# A CSS pseudo-element is used to style specific parts of an element.

For example, it can be used to:

- Style the first letter or line, of an element
- Insert content before or after an element
- Style the markers of list items
- Style the viewbox behind a dialog box


Syntax:
-------
The syntax of pseudo-elements:
______________________________
selector::pseudo-element {
  property: value;
}
______________________________



> CSS - The ::before Pseudo-element
------------------------------------
:
# The ::before pseudo-element can be used to insert some content before the content of an element.

The following example inserts an image before the content of each <h1> and <p> element:

Example: do visit "before_pseudo_element" folder.




> CSS - The ::after Pseudo-element
-----------------------------------
:
# The ::after pseudo-element can be used to insert some content after the content of an element.

The following example inserts an image after the content of each <h1> element:

e.g: do visit "after_pseudo_element" folder.




> CSS - The ::marker Pseudo-element
-------------------------------------
:
The ::marker pseudo-element selects the markers of list items.

The following example styles the markers of list items:

e.g: do visit "marker_pseudo_element" folder.




> The ::selection Pseudo-element
---------------------------------
:
# The ::selection pseudo-element matches the portion of an element that is selected by a user.

The following CSS properties can be applied to ::selection: color, background, cursor, and outline.

The following example makes the selected text red on a yellow background:

e.g: do visit "selection_pseudo_element" folder.

