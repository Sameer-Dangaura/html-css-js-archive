# Already introduced in "js-8_Object/6_Object_Method.js" and "js-12_Object_Constructor/1_intro.js" .

> For this, do must visit :"w3schools"  till the end.

> for 'Explicit Function Binding': call() and apply(), do scroll to last of w3schools.

__________________________________________________________
while seeing:
The Function call() Method
The Function apply() Method
The Function bind() Method                   in w3schools.

# In 'JavaScript Function apply()' lesson
: 
- If you get introduced with:
    Since JavaScript arrays do not have a max() method, you can apply the Math.max() method instead.

    Example
Math.max.apply(null, [1,2,3]); // Will also return 3

# then, do visit "js-25_array_sort/1_intro.md" to know about null in this context.

__________________________________________________________


- The example below calls person1.fullName with person2 as an argument,   this    refers to person2, even if fullName is a method of person1:

Example: do visit "w3schools" .



- With the bind() method, an object can borrow a method from another object.

This example creates 2 objects (person and member).

The member object borrows the fullname method from the person object:

Example: do visit "w3schools" .

