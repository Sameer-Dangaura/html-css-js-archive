> For this, do visit "w3schools" .



# some topics are already introduced:

- Object.entries(object) -> introduced in "js-11_Object_Display/5_display_Using_Object.entries().js" .

- Object.keys(object) -> is kind of similar to        array.keys() , which is introduced in "js-26_array_iteration/6_array_keys().js" .

- Object.groupBy(object, callback) -> is kind of similar to Map.groupBy(object, callback) , which is introduced in "js-43_Maps/3_Map.groupBy().js" .

