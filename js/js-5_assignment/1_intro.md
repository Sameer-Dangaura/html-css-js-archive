> JavaScript Assignment Operators
(Do visit "js-3_operators" also for assignment operators)
:
Assignment operators assign values to JavaScript variables.

# I think, it is basic thing which we had done in C, C++ (like: x+=y, x*=y, and so on...).

And if shift assignment, bitwise assignment operators are needed then you can take reference from w3schools. 
