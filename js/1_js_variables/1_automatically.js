//-> JS allows the automatic declaration of variables by assigning a value to a name without using var, let, or const. 

//-> However, This is bad practice, because it creates global variables and can lead to unpredictable code behaviour.

//Example:

myNumber = 10;
console.log(myNumber);