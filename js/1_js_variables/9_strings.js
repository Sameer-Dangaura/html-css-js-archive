//-> Variable types:
//-> 2) strings:

//-> Strings are sequences of characters used to represent text.

//-> They are enclosed in quotes, either single('') or double(""), and can include letters, numbers, and symbols.

let favQuote = "Let it go";
let address = "daijee-03";
let city = 'Mnr';

console.log(favQuote);  //outputs: Let it go
console.log(address);   //outputs: daijee-03
console.log(city);  //outputs: Mnr