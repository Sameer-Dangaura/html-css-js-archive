//-> Using   var   keyword was the standard way to declare variables.

//-> Variables declared using the   var   keyword have their scope limited to the function within which they are defined, or they are scoped globally if they are declared outside any function.

var myValue = 300;
console.log(myValue);
var myName = 'Hari Parsad Joshi';
console.log(myName);