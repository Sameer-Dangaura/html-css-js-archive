> Conclusion:
:
- > JavaScript variables can be declared using   var,   let,   or   const   , each serving different scopes and use cases,
            with    let   and   const    being preferable 
for modern code due to their block-scoping benefits.